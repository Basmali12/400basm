[build]
  command = "unzip -o netlify-source.zip -d .netlify-build && cd .netlify-build && npm install && npm run build && cp -r dist ../dist"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
